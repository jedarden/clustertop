package syncclusters

import "errors"

// Run regenerates clusters.yaml from a declarative-config checkout. Stub —
// replaced by Phase 5's "Manifest scanner + regenerator" task (bead ct-wds).
func Run(args []string) error {
	return errors.New("not implemented")
}
